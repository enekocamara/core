#version 460 core

layout (location=0) in vec2 aPos;
layout (location=1) in mat4 aModel;
layout (location=2) in vec3 aMaterial;

uniform mat4 uViewProjection;
out vec3 fragMat;
void main()
{
    gl_Position = ViewProjection * aModel * vec4(aPos, 0.0, 1.0);
    fragMat = aMaterial;
}