#pragma once
#include <entt.hpp>

namespace Sandbox{

    //function trigger

    //vision in the world
    //which objects are close
    //means that we need a chunck system
    inline void tick(entt::entity entity, entt::registry& registry){
    }

    struct Npc{

    };
}