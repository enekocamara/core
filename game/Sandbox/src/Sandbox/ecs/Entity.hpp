#pragma once

namespace Syris::ecs{
    //we want managed dlls
    //if it cant be found it will use a default function that does nothing?

    template<typename T>
    void entity_dispacher(){
        
    }
}