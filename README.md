Core is a c++ building tool that aims to work like rust cargo.<br>
It can create new projects, have multiple targets(executables, dynamic libraries, static libraries).<br>
It handles project dependencies using git submodules.<br>
Uses CMAKE to build the project.<br>
It now correctly uses Github Tags for dependency versions.

To be implemented
Automatic documentation generation using Doxigen
Native google test support
