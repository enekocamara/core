Core is a c++ building tool that aims to work like rust cargo.
It can create new projects, have multiple targets(executables, dynamic libraries, static libraries).
It handles project dependencies using git submodules.
Uses CMAKE to build the project.

To be implemented
Automatic documentation generation using Doxigen
Native google test support
